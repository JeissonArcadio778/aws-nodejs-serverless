'use strict'

const prepareOrder = async (event) => {

    console.log('Prepare Order Starting...');

            try {
                
                console.log(event);
                        

            } catch (error) {
                
                console.log(error);

            }

}


module.exports = {
    prepareOrder
}